server:
  port: 9403

spring:
  application:
    name: @project.artifactId@
 # 配置中心
  cloud:
    config:
      fail-fast: true
      name: ${spring.application.name}
      profile: ${spring.profiles.active}
      discovery:
        enabled: true
        service-id: martin-cloud-config
  profiles:
    active: dev

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone:  http://martin-cloud-eureka:9500/eureka/



